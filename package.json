{
  "license": "MIT",
  "scripts": {
    "validate": "tsc --noEmit --project src && yarn eslint . && yarn html-validate src",
    "build": "yarn parcel build src/index.html --public-url .",
    "serve": "yarn parcel serve src/index.html"
  },
  "dependencies": {
    "fft.js": "^4.0.4"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.60.0",
    "@typescript-eslint/parser": "^8.60.0",
    "eslint": "^10.4.1",
    "html-validate": "^11.4.0",
    "parcel": "^2.16.4",
    "typescript": "^6.0.3"
  }
}
